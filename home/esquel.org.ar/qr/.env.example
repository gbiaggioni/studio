# Credenciales de la Base de Datos
# ¡IMPORTANTE! Si tu base de datos corre en el mismo servidor que Docker,
# USA 172.17.0.1 como DB_HOST.
# (127.0.0.1 dentro de un contenedor es el contenedor mismo, no el servidor).
# Si tu base de datos está en otro servidor, usa su IP.
DB_HOST=172.17.0.1
DB_USER=qreasy_user
DB_PASSWORD=secret_password
DB_NAME=qreasy_db

# URL para desarrollo y producción
# En desarrollo local (sin Docker), usa http://localhost:3000
# En producción, usa tu dominio público.
NEXT_PUBLIC_BASE_URL=https://qr.esquel.org.ar
