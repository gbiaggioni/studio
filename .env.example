# Credenciales de la Base de Datos
# ¡IMPORTANTE! Si tu base de datos corre en el mismo servidor que Docker,
# USA 172.17.0.1 como DB_HOST.
# (127.0.0.1 dentro de un contenedor Docker se refiere al propio contenedor, no al servidor).
DB_HOST=172.17.0.1
DB_USER=qreasy_user
DB_PASSWORD=secret_password
DB_NAME=qreasy_db

# URL pública de tu aplicación, usada para generar los enlaces cortos.
# Asegúrate de que no tenga una barra al final.
NEXT_PUBLIC_BASE_URL=https://qr.esquel.org.ar
